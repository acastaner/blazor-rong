server {
    listen 80;
    root /usr/share/nginx/html;
    index index.html;

    # Serve pre-compressed .gz files produced by dotnet publish
    gzip_static on;

    # SPA fallback: unknown paths return index.html so client-side routing works
    location / {
        try_files $uri $uri/ /index.html;
    }
}
